// Veamos la diferencia entre un método y una función...

const numero1 = 20;

// Esto es un Método para convertir un número a cadena de texto
console.log(numero1.toString());

const numero2 = "20";

// Esto es una Función para convertir una cadena de texto a número
console.log(parseInt(numero2));
